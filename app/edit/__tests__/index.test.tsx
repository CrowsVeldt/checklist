import { render } from "@testing-library/react-native";
import EditChecklist from "../index";
import AppProvider from "@/context/AppContext";

describe("<CreateChecklist />", () => {
  test("Index renders correctly", () => {
    // const tree = render(
      // <AppProvider>
        // <EditChecklist />
      // </AppProvider>
    // ).toJSON();

    // expect(tree).toMatchSnapshot();
  });
});
